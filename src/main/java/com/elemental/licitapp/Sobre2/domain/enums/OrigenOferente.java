package com.elemental.licitapp.Sobre2.domain.enums;

/** Como entro el oferente al sistema. */
public enum OrigenOferente {

    /** Importado del dataset de ofertas por proceso de SECOP II. */
    SECOP,

    /** Cargado a mano por el analista (procesos sin ofertas publicadas o ajustes). */
    MANUAL
}
