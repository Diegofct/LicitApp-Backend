-- Baseline: representa el estado actual del schema generado por Hibernate (ddl-auto=update)
-- antes de la adopcion de Flyway. No ejecuta DDL porque se usa spring.flyway.baseline-on-migrate=true.
-- A partir de V2 todas las migraciones se gestionan aqui.
SELECT 1;
